# Project Context

- **Owner:** Pascal van der Heiden
- **Project:** sdd-mini-hack — a 1-hour learning repo teaching spec-driven development (SDD) and agentic engineering with GitHub Copilot. Four scenarios: (1) Greenfield Todo with OpenSpec, (2) Brownfield feature with Copilot Plan Mode, (3) Legacy COBOL modernization with Spec Kit, (4) Personal Assistant with Copilot CLI + Squad + OpenSpec.
- **Stack:** Node.js 20.19+ (vanilla JS / .mjs servers, static `public/` frontends), COBOL (legacy example), Markdown docs, Playwright MCP, Agent Skills, OpenSpec, Spec Kit, GitHub Copilot CLI/Plan Mode/Squad.
- **Goal:** Extend the repo with new SDD use cases/scenarios.
- **Created:** 2026-06-17

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- Repo structure: scenarios in `docs/scenario-N-*.md`, sample apps in `examples/<app>/`, skills in `.github/skills/`, authoring guides in `.github/instructions/*.instructions.md`, MCP config in `.mcp.json` + `.vscode/mcp.json`, verification in `scripts/verify-workshop.sh`.
- User directive: use Speckit workflow only; do not use Superpowers skills.
- Scenario 5 (ETL Pipeline) doc structure mirrors Scenario 3 (COBOL) and Scenario 4 (Squad): prereqs + numbered steps with code blocks + troubleshooting + learning summary. Step 0 is a REQUIRED data endpoint verification before any build. Each Spec Kit phase → one Squad member producing one artifact in order. Cypher's checklist is a hard gate before Seraph implements.
- Scenario 5 prereqs and workflow: Removed GitHub Copilot CLI (runs in VS Code, not CLI). Added Squad UI and Spec Kit companion VS Code extensions (referenced by name, not fabricated IDs). Workflow includes dedicated folder-scoped squad in examples/etl-climate-pipeline/.squad/ (separate from repo-root squad). Step 1 instructs learner to `code examples/etl-climate-pipeline` to activate the dedicated squad. Step 2 (formerly Step 1) updated to reflect pre-scaffolded docker-compose.yml and init.sql ("review and run" instead of "create from scratch"). All subsequent steps renumbered (+1).
- Scenario 5 self-create workflow (2026-06-17): The example folder NO LONGER ships a pre-made `.squad/` — creating it is NOW PART OF THE EXERCISE. New Step 3 "Create your own Squad" instructs learners to run `squad init` in the example folder (isolated from repo-root squad). Added extensive "Prompt for a crystal-clear team" subsection with concrete example prompt and "Tips for clear prompting" guidance. Learners now draft the hire request themselves, naming explicit roles and counts (9 phases + core build team). Added colima documentation to prerequisites (macOS Docker runtime): `brew install colima docker`, then `colima start` before `docker compose up`. Updated Scenario 5 README and Step 2 DB-setup section to reference colima. All remaining steps renumbered (+1): Step 4→Meet Squad, Step 5→Init Spec Kit, etc. through Step 14 Troubleshooting.
- Scenario 5 concise rewrite (2026-06-17): Collapsed 589 lines → 141 lines (target: 150–200). Removed per-phase step-by-step instructions (Constitution→Specify→...→Validation), verbose tables, and SquadUI extension guidance. NOW uses the Squad **custom agent in Copilot Chat** (from `.github/agents/squad.agent.md`) — removed `squad init` / `npm install -g @bradygaster/squad-cli` entirely since Squad is a repo prereq. Simplified to 7 streamlined steps: (0) test endpoints, (1) stand up DB, (2) hire team via custom agent in Chat, (3) init Spec Kit, (4) run Spec Kit process (one prompt to Lead for constitution/spec/plan/tasks/skills, then STOP), (5) validate specs, (6) implement (one prompt to Lead to orchestrate team following speckit.implement), (7) validate pipeline. Removed the long member→phase assignment table and verbose per-role explanations. Updated docs/prerequisites.md and examples/etl-climate-pipeline/README.md to match (no Squad UI extension, just Spec Kit companion + Docker + Python). New "What you learned" bullets focus on: (1) Squad orchestrates full lifecycle from two prompts, (2) verified-source & verified-database-first, (3) spec-first greenfield ETL, (4) real persistence teaching.
